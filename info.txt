
TODO:
-> Add middleware to check if the user is logged in, is the user owner of the account. 

IN PROGRESS:
-> seperate db interaction code to service

COMPLETED: {
    signup 
    -> local
    -> email, password
    -> zod validate 
    -> add user to db
    -> generate verification token with token expire date 
    -> send verification email 
    -> jwt token


    resend logic
    -> email
    -> find by email
    -> block resend email if within block period/time
    -> generate token and token expire 
    -> if token resend count > 3 wait for 10 minutes // set block resend window to 10 min
    -> resend count +1
    -> send verification token via email (nodemailer)


    verify token 
    -> token = req.body
    -> find user by token 
    -> check if user exists or not 
    -> check if the token is valid or not 
    -> set isvalid to true
    -> set null-> token , token expires, block resend window, resend count
    -> save db 
    -> login with google
    -> login with facebook 

    -> cron job-> delete token from db after the token expires
    -> password reset
    -> email, profile management
    -> Routes for login with facebook and google
}





// Recommendations for improving the codebase
// Recommendation: Use custom error types for consistent error handling
// Recommendation: Implement rate limiting for sensitive endpoints
// Recommendation: Add input validation middleware (e.g., express-validator)
// Recommendation: Use environment variables for sensitive configurations
// Recommendation: Implement structured logging (e.g., winston)
// Recommendation: Add API documentation using Swagger/OpenAPI
// Recommendation: Implement request timeout handling
// Recommendation: Add correlation IDs for request tracing
